mixin icon(props, extraClasses)
    -
        var iconClasses = {
            "Icon___xsmall": props.size === "xsmall",
            "Icon___small": props.size === "small",
            "Icon___standard": props.size !== "xsmall" && props.size !== "small" && props.size !== "large" && props.size !== "xlarge",
            "Icon___large": props.size === "large",
            "Icon___xlarge": props.size === "xlarge"
        }
        var ariaLabel = props.label ? props.label.toLowerCase() : false;
    |
    |
    i.Icon(class=[iconClasses, props.glyph, extraClasses], aria-label=ariaLabel)
